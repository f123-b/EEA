{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the M0 desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
