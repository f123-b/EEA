{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M22R deny-by-default capability set; native import dialogs are the only user-mediated file selection capability.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "dialog:default"]
}
