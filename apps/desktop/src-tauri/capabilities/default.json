{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M18E deny-by-default capability set; no filesystem, shell, process, HTTP, clipboard, or environment permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
