# M23R Next Phase

M24 is **not started**. It may begin only after the M23R acceptance evidence is reviewed and the
logical PR boundary is accepted.

The next-phase intake should be limited to explicitly approved work such as:

- deeper language/build/protocol parser coverage for M22;
- native import picker integration;
- complete generic workflow descriptor rendering and stage execution UX;
- native Tauri build and release artifact verification;
- remote CI/PR acceptance using the final M23R commit.

Each item must retain backend authority, immutable source revisions, explicit freshness/error UI
states, and project isolation. No M24 schema or feature work is included in the M23R closeout.
